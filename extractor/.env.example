# Copy to .env and fill in. .env is gitignored; this file is not.
# Each provider reads only its own key: --provider openai needs the first, --provider anthropic
# the second, --provider openrouter the third. A run never requires another provider's key.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OPENROUTER_API_KEY=
