# Solution Wide Instructions

## Routing — read only what the task needs, when it needs it

### Working targets

- Working on typed extraction, `.with_structured_output()`, Pydantic schemas, or output parsing → `extractor/AGENTS.md`
- Working on the terminal chatbot, message history, session stores, or token streaming → `chatbot/AGENTS.md`
- Working on retrieval, loaders, splitters, vector stores, or multi-query/compression retrievers → `rag/AGENTS.md`
- Working on the tool-calling ReAct agent, `@tool` definitions, or the agent executor loop → `researcher/AGENTS.md`
- Working on parallel chains, conditional routing, or the synthesis pipeline → `synthesizer/AGENTS.md`

### This context

- Solution-wide vocabulary → docs/agents/domain.md
- System-wide decisions → docs/adr/
- Issue tracker (issues live as GitHub issues in `jonnyasmith/langchain`, via the `gh` CLI) → docs/agents/issue-tracker.md
- Triage labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) → docs/agents/triage-labels.md
